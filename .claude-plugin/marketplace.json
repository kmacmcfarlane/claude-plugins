{
  "name": "kmacmcfarlane",
  "description": "Claude Code plugin marketplace for Kyle McFarlane — dev tooling and reusable skills.",
  "owner": {
    "name": "Kyle McFarlane"
  },
  "plugins": [
    {
      "name": "kit-dev",
      "source": "./plugins/kit-dev",
      "description": "Maintainer tooling for this plugin ecosystem — skill creation, project scaffolding, upstream kit sync, and factoring analysis (external: the claude-templates repo, which scaffolding reads, and the claude-templates, claude-sandbox and claude-expertise repos, which upstream sync writes)."
    },
    {
      "name": "context-guard",
      "source": "./plugins/context-guard",
      "description": "Context survival for long sessions — depth gate, checkpoint workflow, session rehydration, and token-spend reporting from the local transcripts (soft dependency: statusline, for exact depth)."
    },
    {
      "name": "statusline",
      "source": "./plugins/statusline",
      "description": "Always-on status line — context left, plan usage limits, model and session name at a glance (optional: works with context-guard, which adds its epoch and checkpoint thresholds to the gauge)."
    },
    {
      "name": "statusline-hub",
      "source": "./plugins/statusline-hub",
      "description": "The status-line slot, shared — the hub owns the statusLine command (taking a free slot on the first session, never another tool's without consent), records the context depth and plan usage Claude Code hands the status line into the sensor record other tools read, then runs the display and record hooks other plugins register; where another renderer keeps the slot, its tee command feeds the same record from a ccstatusline Custom Command, a Starship custom module, or a wrapper around your own command. (Soft dependency: statusline — the hub waits for its footer to register as a hub display hook, then takes over its slot.)"
    },
    {
      "name": "dev-flow",
      "source": "./plugins/dev-flow",
      "description": "Plan-first development for agent sessions — investigation series, reviewed plans, verified implementation, a sub-agent dev cycle that carries one change from plan to merge, multi-agent research fan-outs, chain-of-verification fact-checking, and a standing librarian that takes custody of a repo's work: files, dispatches, reviews and lands (soft dependencies: work-items; statusline, for rate-limit reset times; context-guard, for checkpoint and rehydration)."
    },
    {
      "name": "work-items",
      "source": "./plugins/work-items",
      "description": "Repo-durable work items for humans and agents — the wi CLI over one-markdown-file-per-item stores, and the work-source provider interface."
    },
    {
      "name": "chat",
      "source": "./plugins/chat",
      "description": "Conversational skills — structured product research and purchase recommendations."
    },
    {
      "name": "sandbox",
      "source": "./plugins/sandbox",
      "description": "Isolated execution for Claude Code sessions — configure, bootstrap and troubleshoot claude-sandbox containers, with the checkout/worktree convention their launcher follows enforced by a PreToolUse guard."
    },
    {
      "name": "create-repo",
      "source": "./plugins/create-repo",
      "description": "Start a new repo for a thread of work — the directory, git init, a README naming its purpose, claude-sandbox init and a first commit — then one copy-paste command that launches an agent session on it to write its CLAUDE.md and run the thread's first investigation (external: the claude-sandbox tool, degrading to a plain claude launch without it; soft dependencies: kit-dev, to scaffold a claude-templates template as the goal; sandbox, to troubleshoot a failed init or launch)."
    },
    {
      "name": "ralph",
      "source": "./plugins/ralph",
      "description": "Unattended agent loops over a backlog — the backlog.yaml workflow for ralph runs. Requires the claude-sandbox tool (see the sandbox plugin); consumes a work source through the work-source interface, with backlog.yaml as the default provider (soft dependency: work-items, whose work-source provider interface it implements)."
    }
  ]
}
