{
  "description": "statusline: keeps the plugin-data current-hooks symlink pointing at this version's hooks, so the statusLine command in settings.json stays on an update-stable path.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p \"${CLAUDE_PLUGIN_DATA}\" && ln -sfn \"${CLAUDE_PLUGIN_ROOT}/hooks\" \"${CLAUDE_PLUGIN_DATA}/current-hooks\" >/dev/null 2>&1; true"
          }
        ]
      }
    ]
  }
}
