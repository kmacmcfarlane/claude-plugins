{
  "description": "statusline: keeps the plugin-data current-hooks symlink pointing at this version's hooks, so a statusLine entry an earlier version installed keeps drawing until the hub takes it over; then registers the footer as a statusline-hub display hook (its hooks.d manifest, refreshed every session) and prunes old sensor files. It writes no settings: the statusline-hub plugin owns the statusLine entry.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p \"${CLAUDE_PLUGIN_DATA}\" && ln -sfn \"${CLAUDE_PLUGIN_ROOT}/hooks\" \"${CLAUDE_PLUGIN_DATA}/current-hooks\" >/dev/null 2>&1; true"
          },
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/session_start.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
