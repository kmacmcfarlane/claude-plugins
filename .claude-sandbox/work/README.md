# Work items

One markdown file per item, managed by `wi` (the `work-items` plugin: plugins/work-items/skills/work-items).
Start with `wi prime`, then `wi show <id> --brief` for the item you will work.
Format: plugins/work-items/skills/work-items/references/format.md
