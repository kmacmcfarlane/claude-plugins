# Work items

One markdown file per item, managed by `wi` (claude-kit skills/work-items).
Start with `wi prime`, then `wi show <id> --brief` for the item you will work.
Format: claude-kit skills/work-items/references/format.md
