---
id: dev-cycle-design-resume-whole-split-from-e770
title: "dev-cycle: design § Resume whole (split from 426a)"
type: feature
status: doing
priority: 3
parent: dev-flow-new-dev-cycle-skill-investigate-07c3
owner: unknown@360f41058e92
claimed: 2026-09-22T17:54Z
created: 2026-09-22
updated: 2026-09-22
refs:
  - 426a answer 59
---

Split from 426a by operator answer 59(a), 2026-09-22. 426a lands review <branch> mode without § Resume; this item designs the resume state machine as a whole in dev-cycle plan mode before any build. Prior work (kept in main's history once 426a merges — git show <sha>:plugins/dev-flow/skills/dev-cycle/references/bindings.md): fa4ef02 (first cut), d802a19, 58bece4, 0f0a254, 54f022c (last full version). Feedback: 426a's body holds six review rounds verbatim (sections 'Review round 1'…'Review round 6'); the open mediums at 54f022c are (1) standalone 'recorded but unanswered → wait' deadlocks a resume in a new session (re-ask via the standalone channel; only a caller's persistent channel waits), (2) review mode never re-reviews after a recorded 'no' once the author moves the branch (rules 5/6 need rule 4's stale-sha test); low: rule 7 reuses an answer recorded before the latest BLOCKED run. Acceptance: a plan series whose 00 consolidates the six rounds into requirements + a state table, reviewed CLEAR before any implementer dispatch.

## Handoff
- doing: plan fix round 3 running (planner a27e7f6636698e4e5, opus, serial 03): the dispatch-permission tag settled by the librarian, the rest bookkeeping
- next: serial 03 → a short delta check with reviewer acf4adcf179bf47b2, then F1
- blocked: —
- learned: —
- from 426a final review (low): the shipped dev-cycle bindings.md '## Resume' heading points at this item's id, which marketplace installs cannot reach; the redesign replaces it (or, until then, 'not specified yet: an interrupted run restarts at Step 0').
dispatch: planner opus — plan mode, no worktree; series .claude-sandbox/investigations/e770-dev-cycle-resume/

## Notes
- 2026-09-22 claimed by unknown@360f41058e92

## Plan result (opus, 2026-09-22) — series .claude-sandbox/investigations/e770-dev-cycle-resume/ (00_initial.md, INDEX.md)
Diagnosis: the removed version was a rule list where each rule carried its own copy of the cross-cutting tests (cap, answered, staleness, channel), so every review found another rule with the wrong copy. Fix: compute those facts ONCE before the table is read. 19 requirements (R1–R19) each citing its round; phase lines (dispatch/return/verdict/landed) vs riders; reduction LANDED → PHASE → FRESH → ROUNDS → GATE with CHANNEL as a binding; one 12-row state table (S0–S12), a 4-row gate table, a 5-step liveness probe. r6 medium 1 settled by deleting "wait" entirely (ephemeral channel re-asks the same decision verbatim; durable hands back via wi handoff --blocked and the caller's idle turn resumes). r6 medium 2 settled by making a stale verdict its own state that spends findings AND answers — reviewing needs no permission, only dispatching onto the author's branch does. Line shapes: restore return:/landed:, add `agent: <role> <id> round <n>` and `baseline:`, widen target: and decision: (self-contained + options), generalise `at <sha>` → `at <token>`. Caller split: dev-cycle owns shapes/reduction/table/gate/probe; the caller owns queue, channel + durability, waiting, and carrying agent ids. Features F1→F4 strictly serial: F1 shapes (opus), F2 channel durability (opus), F3 § Resume whole (opus), F4 caller boundary (sonnet, opus review). No fable signal.
librarian decisions on the planner's questions: Q2 (a) → (b) accepted: § Resume lives in a new references/resume.md with a pointer from bindings.md (a state machine is not a binding; bindings.md is already 272 lines). Q3 (a): answer scope expressed positionally ("in force only when no phase line follows it") plus the named review-mode run-scoped exception. Q4 (a): a store-less run states its resume limit in the Step 0 summary; no new durable sink. Q5 (a): the interrupt walk is a review record, not a shipped walkthrough. F4 stays sonnet with an opus review; its files are two and mechanical once F3 lands.
decision 64: does a pending decision ever block a resume? — (a) never: an ephemeral channel re-asks the same decision now, a durable one hands back to its caller [recommended by the planner; goes further than review round 6, which proposed that only a caller's persistent channel waits — the planner's point is that a durable channel with no reader deadlocks the same way]; (b) keep "wait" on a durable channel with a timeout.
dispatch: plan reviewer opus — dev-cycle plan-review variant on 00_initial.md

## Plan review round 1 — NEEDS_CHANGES (opus) on 00_initial.md
Coverage: every medium+ finding from r1–r6 maps to a requirement; two map to a requirement the model does not close (r1 m9 → H2, r5 m2 → H3). Format conforms (Confirmed Assumptions present).
- [high 1] :199 S11's AT_CAP arm carries no phase constraint, so any record with 4 counted verdicts also matches S3/S4/S5/S6/S7/S12 — the waived-cap-then-dispatch path (this item's own history, answers 51/57) matches two rows with contradictory actions. 54f022c had "first that applies wins"; :183 drops precedence without replacing it. Pass: a phase constraint on S11's AT_CAP arm, or an explicit read order.
- [high 2] plan mode falls through: S4/S5/S6 key on `return: implementer`, a planner return matches nothing; Step 3.5 (the only `return:` writer) never runs in plan mode; F1's acceptance "target: carries … worktree path in every mode" is unsatisfiable (plan mode has no worktree).
- [high 3] S6/S12 read "a permission denial", which no line writes — :317-332 has no field for it; the plan's own R4 grep (:340-343) fails on its own model. This is r5 m2 reproduced with a new fact.
- [medium] CURRENT undefined for non-phase verdicts, so the one cap test is not evaluable (:161 vs :168-171); GATE ephemeral "re-ask now" contradicts the no-asking-after-heavy-analysis rule two rows above (:220 vs :221); the probe is a second writer and a resume-of-a-resume can double-dispatch (no state or detector for two live agents); R18 (a scratchpad sink from another session) has no state; the brief variant is decided in two cells with different answers; F2 is not independently landable (its exception names S10, which F3 introduces); F4's two-line scope cannot reach its own acceptance — four restatement sites exist (librarian SKILL.md:95 and :108, ending-the-session :17-18, :79, :101).
- lows: a misattributed quote drives F4's edit at the wrong line; probe step 5 has no reference point on a first dispatch; `at <token>` already ships for plan mode; the no-fable claim contradicts F3's fix-round-3 rule.
- reviewer: "computing the facts once" is directionally true and worth keeping — fold ROUNDS' freshness, S12's second count and the brief-variant choice into the reduction and it becomes true. Run the interrupt walk on the revised table as part of the re-plan, not at F3's review.
- on decision 64: only one cell changes either way; (b) also reopens the split's "waiting → caller" row and duplicates what `wi needs-input` + the Groom table already do. (a) stands.
dispatch: planner opus — plan fix round 1 (resume); revision as serial 01 with Supersedes

## Plan fix round 1 (opus) — serial 01_partition-and-walk.md (438 lines), Supersedes names 15 statements in 00
H1 closed by re-keying the table on PHASE, which partitions the record six ways (NONE/RIDERS/LANDED/DISPATCH/RETURN/VERDICT); AT_CAP stops being a state and becomes an axis inside the verdict group only, so the waived-cap-then-dispatch path is S3 and nothing else — rows cannot overlap by construction. A latent bug fell out: a CLEAR current at HEAD after four rounds now lands instead of deadlocking (that flaw has been in the cap test since 58bece4). H2 closed: group C keys on STATUS with <role> as a field; SKILL.md Step 1 becomes the planner's return: writer; target:'s third field is a workspace (worktree path, or the series path in plan mode). H3 closed: BLOCKED phase lines carry a reason from a closed set (permission | setup). Mediums: fresh(v) over any verdict; the ephemeral gate arm becomes "end the turn, ask at the start of the next" (obeys bindings.md:197-205, still not waiting); the probe no longer writes (it routes its report into Steps 3.5/4.5); a new S13 "two live agents" (stop, raise, kill nothing) and S0b "sink unreachable + live remnant", which also closes a silent-restart hazard where a lost scratchpad read as S0 and would have re-dispatched over a live worktree; VARIANT computed once; F2's exception restated without naming a state, so it lands before F3; F4's scope is five sites. The reduction is now eight facts and no row recomputes any. The interrupt walk is § 7: 22 rows, every state reached — rows 17b and 20 are the two that would have caught the highs.
librarian: the planner numbered a new question "65", which collides with the F3b decision of that number; renumbered here as 66.
decision 66: S13, two live agents on one worktree — (a) always a decision; the cycle never kills an agent on its own say-so [recommended by the planner and the librarian: killing on the cycle's own judgement is what S13 exists to prevent]; (b) keep the newest and report the other.
dispatch: plan reviewer opus — plan-review round 2 (resume) on serial 01

## Plan review round 2 — NEEDS_CHANGES (opus) on serial 01
All three round-1 highs genuinely closed; the reviewer re-ran its own walk and could not reconstruct H1 or H2. Group D verified exhaustive and disjoint by enumeration; the generation scoping verified on four traces; the probe's no-write routing closes round-1 M6; rows 17b and 20 resolve as claimed. Every round-1 finding closed; Supersedes substantively accurate.
- [high A] fact 7 computes GATE over decisions after the last phase line, but the review-mode dispatch permission is scoped to the verdict it was given under: after decision/answer → dispatch → return → verdict, GATE reads NONE and S9 re-asks the operator for permission already granted, every fix round; walk row 14c resolves it by hand. Pass: fact 7 carries the exception in its own definition.
- [medium B] group B's rows are selected by a probe outcome, not a record fact, so S3 and S13 overlap and "a reviewer can check this by reading the table alone" is false for group B; group P's remnant check is likewise not one of the eight facts. Pass: make the probe outcome group B's axis (three rows) or move S13 into § 6d; add the remnant as a ninth fact.
- [medium C] a legacy `return: … BLOCKED` with no reason matches none of group C's three rows (group D has an "otherwise", group C does not). Pass: a missing reason reads as permission.
- [medium D] fresh() has no value when the baseline or the workspace is missing (a plan verdict with no baseline:, a deleted worktree), and three of group D's axes need it. Pass: unreadable → STALE.
- [medium E] RETRIES < 2 allows one retry where fix-loop.md:12-13 (cited by § 6f as its own authority) and 54f022c's rule 7 both say twice. Pass: RETRIES < 3, or state the narrowing.
- [medium F] S0b fires only when the sink is unreachable, then routes through the GATE, which must read and write that sink. Pass: S0b reports and stops.
- [medium G] VARIANT's last row reads "a merge conflict is in play" — no line, no position — so a round-1 conflict: overrides VARIANT forever. Pass: key it on a conflict: after the last verdict.
- [medium H] group A never consults the GATE, so a pre-dispatch pending decision (the fable-fallback window) is dropped by S1's "re-ask nothing". Pass: S1 consults the GATE, or R8 is scoped to answered questions.
- [medium I] the walk's completeness claim is false: S6b is never walked, and three of S11's four arms are unwalked.
- lows: "five sites" vs six listed (an acceptance criterion); the landing-at-cap fix is credited to a latent flaw since 58bece4, but 54f022c's ordered rules landed a current CLEAR — the deadlock came from 00 itself; eight Supersedes line citations drift; bindings.md:143-147 is the findings: bullet, not verdict:; no Blast Radius delta.
- reviewer on trajectory: rounds 1–6 each found a defect in the architecture; this round found one contract with two answers and four holes at the partition's EDGES. The partition survived every attack, including all ten the librarian named. Not evidence for another design pass — amend 01 or add a short 02, then re-review the delta only.
dispatch: planner opus — plan fix round 2 (resume); serial 02 carrying HIGH A and mediums B–I, then re-run the walk with the S6b row

## Plan fix round 2 (opus) — serial 02_partition-edges.md (339 lines), Supersedes names 13 statements in 01
High A closed in fact 7's own definition: state-scoped questions look after the last phase line; the one run-scoped question (review mode's dispatch permission) takes the most recent pair anywhere, in force unless a `spent:` line recorded after it names it — walk row 14c no longer resolves by hand. Working it through surfaced a deeper form the review did not name: 01 § 6c's own sentence ("in force until the verdict it was recorded under stops satisfying fresh()") spent the permission on the run's OWN fix round. Three purely-derived tests were tried and rejected (last-return-sha, HEAD-equals-recorded-tip, ancestor walk) — each gets one of the three traces wrong, because "already spent" is history the record cannot replay; hence one new rider, `spent: <decision>`, one writer, one reader, added under the design's own R4 discipline. B: two new facts — LIVE (probe outcome one|none|many) gives group B three rows, REMNANT gives group P a four-row table — so every group selects on computed values and "a reviewer can check by reading the table alone" is true everywhere (reduction 8 → 10 facts). C: a missing BLOCKED reason reads as permission, in the field's definition. D: unreadable fresh() reads as STALE, with the chain traced to confirm it escalates rather than lands. E: RETRIES < 3 (01 had silently narrowed a shipped rule). F: S0b reports and stops. G: the conflict row keyed on a conflict: after the last verdict; the secret-rebuild case leaves VARIANT rather than gaining a new shape. H: S1 consults the GATE and R8 is scoped to answered questions. I: nine walk rows added, three amended, and the false completeness paragraph replaced by a state → walk-row coverage map plus a guard index.
Lows: six sites; the 58bece4 credit retracted (the deadlock was 00's own and never shipped); eleven drifting citations corrected in a table, not eight; bindings.md's verdict: bullet is :136-142; Blast Radius delta added.
Scope: one new shape (`spent:`), written only in review mode with a recorded permission; `rebuild:` deliberately not added. Legacy-record degradation is now stated and every default escalates (missing rider → not recorded, missing reason → permission, unreadable fresh() → STALE); none lands and none double-dispatches.
librarian: the planner again numbers the S13 question "65" — it is decision 66 here (65 is F3b's repo-visible channel).
dispatch: plan reviewer opus — plan-review round 3 (resume), delta only, on serial 02

## Plan review round 3 — NEEDS_CHANGES (opus) on serial 02
The `spent:` rider is warranted: the reviewer constructed four derived tests and each fails a trace (fresh()-of-the-recording-verdict is spent by the run's own fix round — the planner's own deeper diagnosis, which the reviewer credits; most-recent-verdict-fresh and HEAD-equals-recorded-tip both resurrect a spent "no" after S10's fresh review; the ancestor/coverage walk fails the commonest interrupt, a dispatch with no recorded return). It adds a fifth argument 02 should make: every derived test reads git in the workspace, and fact 3 has just conceded fresh() is unreadable when the workspace is gone, so all of them inherit that degradation while a recorded line does not. LIVE and REMNANT make groups B and P true partitions, with no new recomputation; every state is walked and S6b now has a row.
- [medium 1] "matching q" is a new, undefined predicate. In the run-scoped arm it is load-bearing: "anywhere in the record" includes every cap waiver, NEEDS_CONTEXT answer and fable-fallback ask, several of which read "yes", so a resume could read a cap waiver as the dispatch permission and dispatch onto the author's branch (R13, the guarantee round 3's high was about). 00 Q3(b) rejected tagging decisions when scoping was purely positional; the run-scoped arm disproves that premise for exactly one question.
- [medium 2] `spent:` is assigned to F1, where it has neither a write site nor a read site (both are in F3), so F1 cannot pass its own R4 acceptance. Move it to F3 or amend F1's acceptance.
- [medium 3] the four rejected derived tests are not in the plan of record — the next reader will re-propose fresh()-based derivation and rediscover trace 1 the hard way. Four rows and a sentence.
- [medium 4] the walk contradicts RETRIES < 3 in two places (row 18b unamended; row 26 internally inconsistent) — and row 26 exists to walk the row that changed.
- [medium 5] row 17b — the row named as high 1's evidence — is absent from the coverage map and still says S3, which no longer exists; row 30 resolves to two states; row 22 is mapped under a variant the walk does not contain.
- [medium 6] the guard index covers ten of nineteen requirements with no account of the other nine, five of which are exercised by existing rows.
- lows: the `spent:` writer clause contradicts its own walk rows (S11 never dispatches); LANDED has no stated value when the sink is unreachable; REMNANT still omits plan mode's remnant (the series path) — round 2's LOW J; the "every fact computed before the table is read" invariant is false for the parameterised GATE; two of § 11's own citation corrections are off at the tail; the S13 question is decision 66 here, not 65; three small supersession gaps; Deployment notes still absent.
librarian settles medium 1: tag the one run-scoped decision — `decision: dispatch-permission — <question> — options: …` — and have `spent:` name that identifier rather than prose. `spent:` needs a referent regardless, and the tag is scoped to the single exceptional question.
reviewer's disposition, accepted: fold the rest into a short 03 and treat the result as CLEAR on a delta check rather than another full round; the model has survived three adversarial walks including every construction from rounds 1 and 2.
dispatch: planner opus — plan fix round 3 (resume), serial 03

## Plan fix round 3 (opus) — serial 03_referent-and-bookkeeping.md (362 lines)
Medium 1 implemented as settled: `decision: dispatch-permission — <question> — options: …`, with answer: and spent: naming the same identifier, so fact 7's run-scoped arm matches a literal identifier and a cap waiver answered "yes" can never be read as permission to touch the author's branch; a fourth trace exercises exactly that. Recorded as a one-tag exception, not a kind system, with 00 Q3(a) still governing every other decision. Medium 3: the four rejected derived tests are in the plan of record as a table (test · trace it fails · why) plus the fifth argument — every derived test reads git in the workspace, and fresh() is unreadable when the workspace is gone, so all four have no value there while a recorded line survives. Medium 2: spent: moves to F3 (both writer and reader live there), F1's acceptance is scoped to shapes whose writer is a SKILL.md step F1 ships, and the identifier lands in F2. Medium 4: row 18b corrected (RETRIES 2 re-dispatches) with a new 18b′ for the third BLOCKED; row 26 reworded. Medium 5: row 17b resolves by group B then LIVE and is mapped; row 30 split into 30a/30b; row 22b added. Medium 6: the guard index is complete over all nineteen — fifteen walked, four named structural with their evidence. Lows all closed, including plan mode's remnant (a series directory carrying serials), the restated invariant, the two tail-off citations, decision 66 not 65, the three supersession gaps, and Deployment & Rollout Notes separating the real ordering edges (F2→F3 for the identifier, F1→F3 for the phase lines) from file-conflict ones, with any prefix of F1→F4 a safe stopping point. New acceptance artefact: the identifier test — `dispatch-permission` appears in exactly four places and nowhere else. Legacy records stay safe in the conservative direction (an untagged pair is not found, so the question is asked again).
dispatch: plan reviewer opus — delta check of serial 03 (the last; on CLEAR the plan of record is 00-03 and F1 dispatches)

## Plan delta check round 4 — NEEDS_CHANGES (opus) on serial 03; F1 UNBLOCKED
None of the findings touches F1 (its scope after 03's rescoping is target:, agent:, return:, verdict:, baseline:, landed: and the BLOCKED reason), so F1 dispatches now; the corrections go in a short 04 or F3's brief, with no further review round. Verified: untagged legacy falls conservative in both directions (R13 holds); the cap-waiver trace is correct; the arithmetic (18b re-dispatches at 2, 18b′ blocks at 3, row 26 needs a third) is right; the coverage map is complete over 18 states and 43 rows; the guard index arithmetic holds; § 14's four derived tests match the reviewer's own constructions; group P is exhaustive with line-derived facts marked absent, not false; Deployment notes identify F2→F3 as a real dependency.
- [medium 1] the identifier appears in FIVE places, not four: listing `spent:` in bindings.md § Record line shapes puts the whole payload there, so F3's reviewer runs the acceptance grep, finds five, and either fails the feature or waives the test. Fix: the bindings.md entry is a NAME-ONLY pointer, and the count stays four. Two riders for F2's acceptance: the identifier binding sentence lives on the decision: shape in bindings.md, not in SKILL.md § Usage (or the grep finds five); and a caller's `answer N:` does not carry the tag (the numbered pair matches by N).
- [medium 2] row 17b resolves to two states (S3a and S3b), against 03's own "every walk row resolves to exactly one state" — the defect this serial fixed twice elsewhere (row 30 split, 22b added). Fix: split 17b by LIVE, or map it under group B with row 29's annotation style.
- [medium 3] REMNANT's new fourth remnant refuses a legitimate re-run: a store-less `/dev-cycle <slug> plan` in a new session, where the series already exists, reads SINK unreachable + REMNANT present → S0b "not resumable", but an existing series is a stated INPUT (SKILL.md § Usage takes a slug; plan-mode NEEDS_CHANGES re-dispatches to revise by a new serial — this very series is four serials produced that way). Fix: a series directory counts as a remnant only when the target was not given as a slug or plan path.
- lows: a permission raised but never answered survives a staleness event (spent: spends answers, so an unanswered pair stands and is re-asked verbatim under a new verdict — fails safe); R3's evidence line names the coverage map for a property only the partition test discharges; R10's named evidence is not in F3's acceptance; the state-table citation is 00:181-211, between 02's and 03's numbers.
- reviewer's watch item for F3: this is the second detector widened to close a gap that then caught a legitimate artefact (the first was 01's S0b). If a third appears, state the general rule once — an artefact is a remnant only when this run created it and the target was never handed it.
librarian settles medium 1's riders as the reviewer frames them: name-only in bindings.md, the binding sentence on the decision: shape, and no tag on a caller's answer N:.
dispatch: implementer opus — F1 (record line shapes and their writers)
dispatch: planner opus — serial 04, corrections only, no further review round

## Serial 04 (opus) — corrections only; PLAN OF RECORD is 00–04 composed in order
Medium 1: the four settled statements are in 04 § 18.1 and in F2's acceptance (bindings.md's spent: entry is a name-only pointer; the count stays four; the binding sentence lives on the decision: shape, since § Usage would make five; a caller's answer N: carries no tag, so librarian-mode stays untouched). Medium 2: row 17b split by LIVE into 17b-i (alive → S3a) and 17b-ii (gone → S3b), with high 1's point stated as a property of the GROUP — ROUNDS is not read in group B — rather than of either arm. Medium 3: a series directory is a remnant only when the target was not given as a slug or plan path, cited against SKILL.md:43-45 and :119-121; the other three remnants stay unqualified because none can be handed to a run as a target, which is the distinguishing property. Lows: spent: now spends a dispatch-permission pair answered or not (the staleness event destroyed the findings the question was about, so raising it afresh carries the new findings); R3's evidence split by half (exclusivity and no-fall-through → the partition test; reachability → the coverage map); R10 promoted to an explicit F3 acceptance line; the citation is 00:181-211. The watch item is recorded as § 19 with an explicit trigger rather than asserted now — two cases are not a pattern, and a rule asserted over two is the restatement 00 § 5 warns about; F3's reviewer raises it if a third appears.
Nothing in F1's scope, so the dispatched F1 is unaffected.
