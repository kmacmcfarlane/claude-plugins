---
id: context-guard-8cc2-f3b-where-handoff-md-a49b
title: "context-guard 8cc2-F3b: where HANDOFF.md lives (per-session memory + repo handoff file)"
type: feature
status: doing
priority: 2
deps:
  - context-guard-8cc2-f3a-re-inject-handoff-5126
parent: context-guard-turn-gate-8cc2
owner: unknown@360f41058e92
claimed: 2026-09-22T17:54Z
created: 2026-09-21
updated: 2026-09-22
---

Port plan .claude-sandbox/investigations/8cc2-turn-gate-port OQ1 — blocked on operator decision 47.

## Handoff
- doing: F3b-1 and F3b-2 landed
- next: 0836 FIRST (the read path, confirmed), then F3b-4, then F3b-3, then F3b-5 on decision 65
- blocked: —
- learned: —

answer 47 (operator 2026-09-22) — see 8cc2: per-session manifests only (C); checkpoint goals continue|handoff (land dropped); handoff manifest not under .claude-sandbox; final turn always prints the full HANDOFF.md path; handoff prints location + example continuation commands (`/compact ...`, and a `then <next-skill>` one when args name it). This changes the F3b design and the checkpoint skill: planner writes serial 04 of 8cc2-turn-gate-port folding it in after F3a lands.
- from H1 (9852) fix r1, 2026-09-22 — F3b input: lineage entries need a link timestamp so the mark step can tell a fork child's own rewrite from a still-running fork parent's rewrite after the fork.
- (from H1 r2) the same gap applies to adoption: a still-running handoff author's rewrite after the adopter's Read; record a link/adoption time.
dispatch: planner opus — serial 04 of 8cc2-turn-gate-port (plan mode, no worktree): fold answer 47(c) + the H1/H5/lineage-timestamp inputs

## Notes
- 2026-09-22 claimed by unknown@360f41058e92

## Plan result (opus, 2026-09-22) — serial 04_per-session-manifest.md in .claude-sandbox/investigations/8cc2-turn-gate-port/ (INDEX regenerated)
One manifest per session at ${CLAUDE_CONFIG_DIR:-~/.claude}/claude-kit/handoff/<safe_sid>/HANDOFF.md — a directory per session so the basename stays HANDOFF.md (keeps lineage's pre-filter and the operator's "full HANDOFF.md path" literal). A successor reaches it by the /clear link (H5, by lineage id), by a whole-file Read of the printed absolute path, or through wi handoff / the series for anything repo-durable; nothing is written into a repo. rehydrate gains resolve_manifest: own → inherited (newest lineage pin whose {owner, sha} still matches) → adopted → legacy → none. A new `top:` field records the manifest's repo. Re-pointed: rehydrate manifest_path/read_manifest, lineage's pin and Read-identity test, mark_checkpoint.stamp_manifest, read_list's top, H5's linked_clear_pred, the docs. Input (i): the lineage timestamp is no longer needed — per-session paths close H1's fork-parent/handoff-author residual by construction, and handoff-format's F3b caveat is deleted. Input (ii): owner == sid is subsumed by the path; the ownerless clause survives only on the legacy path. Also lands next_skill:, the one-line opener naming the Read tool, the always-printed absolute path with continuation commands, and goal options continue|handoff. Migration: a legacy repo manifest that is ours is copied into the store once, byte-identical; a foreign one gives one notice; nothing ever rewrites or deletes a repo HANDOFF.md. Features F3b-1 store+resolve (opus), F3b-2 mark on the own path (opus), F3b-3 migration (opus), F3b-4 checkpoint contract incl. two plugins (opus), F3b-5 the repo-visible channel (blocked on the decision below). Any build touching L.mark_checkpoint, context_warn.decide or hard_applies sends its reviewer to fable.
librarian decisions on the planner's questions: OQ2 (a) — the hook keeps reading `mode: land*`, the skill stops writing it. OQ3 (a) — no pruning of the manifest store for now; filed with the ledger as a follow-up. OQ4 (a) — keep the lineage `at` field (one optional value, useful for the migration window and diagnosis).
Concerns recorded: a handoff between two trees with DIFFERENT CLAUDE_CONFIG_DIRs now shares no file — the printed absolute path may not resolve in the reader's container (only the stub option would fix it; the work item is the workaround). A bystander session now sees nothing on SessionStart instead of a foreign header, which was also how an operator noticed a manifest existed (playbook line in F3b-4). 0509's stage-boundary handoff and librarian-mode's trackInHost manifest commit both change: the manifest leaves git.
decision 65: your answer 47 said a handoff manifest "should not go in .claude-sandbox, because not all consumers use claude-sandbox". The plan reads that as the per-session config-dir store, with nothing in the repo. Which did you mean? — (a) nothing in the repo; the manifest lives in the config dir and its absolute path is printed [recommended by the planner and the librarian]; (b) also drop a repo-root pointer stub — absolute path, author, timestamp, opener line, no body, so it can never be injected as memory; it gives a git-visible trace and something a non-Claude-Code consumer can find; (c) the whole manifest at <repo>/HANDOFF.md in handoff mode, which brings back the overwriting between sessions that 47 removed.
dispatch: plan reviewer opus — dev-cycle plan-review variant on serial 04

## Plan review round 1 — NEEDS_CHANGES (opus) on serial 04
Design sound, answer 47 read correctly, format compliant, every file:line citation checks out.
- [high] F3b-3's migration copy turns a SEALED foreign manifest into an unsealed own one: is_ours covers ownerless/pinned/adopted, all re-evaluated each SessionStart today; after the copy they never are. B adopts A's manifest, copies it, A rewrites with a CORRECTION → B reads its frozen copy forever (today it falls to the foreign header). And an operator's ownerless hand-written HANDOFF.md is copied by EVERY session, then re-dated FRESH and re-owned because the copy resets mtime inside STAMP_WINDOW_S. Pass: copy only when owner == sid; leave the other arms as a live re-sealed read.
- [high] the feature order leaves main with no rehydration for two merges: F3b-1 re-points resolve_manifest while the legacy arm is gated on F3b-3, so every existing (repo) manifest becomes unreachable; F3b-2 then stamps the store path while the skill still writes the repo path, so checkpoints break end to end. Pass: put the read-only legacy fallback in F3b-1 (copying stays in F3b-3).
- [medium] resolve steps 2/3 key on `owner` (frontmatter content) where they must key on the linked session id — manifest_path(pin.owner) can point at a third session's dir exactly during migration; manifest_adopted needs the store sid recorded from the path that was Read.
- [medium] adopt_fork_state is a fourth pin site, missing from the re-point table, and forces a call-order change in main (resolve_manifest must run after the link/adopt block) — F3b-1's fork acceptance cannot pass without it. mark_checkpoint.session_warnings also reads the repo file and would warn on every mark.
- [medium] the "F1 is disjoint" Confirmed Assumption is false: F1 touches checkpoint SKILL.md, the playbook, ending-the-session.md and README.md — F3b-4's own list.
- [medium] the cross-container workaround is asserted, not specified: wi handoff carries four fields and cannot hold Holds, In flight, Copy forward, Read in full or CORRECTION/REFUSED. Say which the handoff block must duplicate, or say the workaround is lossy.
- lows: contradictory fallback for an unusable top: (cwd toplevel vs "(head unverified)"); read_list's top mischaracterised; F3b-2's --path edits mark_checkpoint.main, which calls L.mark_checkpoint — either a separate entry point (keeps opus/opus) or the reviewer routes fable; the fable rule as phrased sets the reviewer, where the routing doc sets the implementer and rule 4 follows.
- reviewer answers the librarian's cross-container worry: NOT broken here — CLAUDE_CONFIG_DIR is unset, so the store is /home/rt/.claude/claude-kit/handoff/, the host-mounted dir every sandbox shares; a printed absolute path resolves in every sibling sandbox, and the librarian rehydrates from wi prime + git anyway. The break is a tree exporting its own config dir, another host, or a non-sandbox reader.
- reviewer on decision 65: the bystander loss is the strongest argument for (b) — after F3b a fresh session in a repo has no mechanism at all to learn a handoff is waiting, and the operator must paste a config-dir path they cannot guess. Also notes /home/rt/.claude/claude-kit/handoff/ already holds three flat <sid>.md files from an abandoned layout (no collision; say they are ignored).
- note: collapsing _claimable drops the "argv id ≠ env grants nothing" rule unless the store path is manifest_path(this_session(sid, environ)) — state it, or H1's closed finding reopens.
dispatch: planner opus — plan fix round 1 (resume); revision as serial 05 with Supersedes

## Plan fix round 1 (opus) — serial 05_legacy-read-only-and-link-keyed-resolve.md (352 lines), Supersedes names 11 statements in 04
High 1 closed: the copy is restricted to owner == sid (the one arm with no seal to lose) and preserves mtime, so an adopter never freezes an author's manifest past a CORRECTION and an ownerless hand-written HANDOFF.md is copied by nobody; ownerless/pinned/adopted/foreign stay live, re-sealed, read-only reads. High 2 closed: the read-only legacy fallback moves into F3b-1 and F3b-2 stamps store-else-legacy, so every merge point is green (after F3b-1 an unmigrated repo behaves byte-for-byte as main). Resolve steps 2/3 key on the linked session id (lineage[n].sid, a new manifest_adopted.sid from the Read path); a pre-F3b pin falls through to legacy — the fail-safe direction. adopt_fork_state is the fourth pin site (loses its version= parameter; main resolves after the link/adopt block, with a fork test that only passes under the new order); session_warnings is the fifth. --path moves out of mark_checkpoint.main into a new hooks/handoff_path.py, so F3b-2 stays opus and H1's tests re-run unchanged; the store path is stated as manifest_path(this_session(sid, environ)), keeping H1's "argv id grants nothing" closed by the path. The three flat <sid>.md files in the store are ignored. Cross-container corrected: fine for sibling sandboxes sharing /home/rt/.claude; broken only for a tree with its own CLAUDE_CONFIG_DIR, another host or a non-sandbox reader — and the wi handoff workaround is LOSSY, naming what must be duplicated (Holds, In flight, Copy forward, Read in full, CORRECTION/REFUSED) and what is lost (Goal, Doing, Scrolls, other Aware-of tags). F1 is recorded as NOT disjoint: it lands first or F3b-4 takes the conflict round.
librarian on the planner's new question (legacy_notice for a session that already has a store manifest): no — a session with its own manifest is not told about a file that is not its memory.
dispatch: plan reviewer opus — plan-review round 2 (resume) on serial 05

## Plan review round 2 — NEEDS_CHANGES (opus) on serial 05
Round-1 highs, mediums and lows all closed, and closed well (the adopter-freeze and ownerless probes re-run: both fail-safe, the ownerless case now better than main — the operator's file survives on disk where today the first checkpoint overwrites it). The link-keyed resolve holds up: a pin always has an address, a pre-F3b pin falls through both ways, an adoption with no sid seals only the legacy arm. --path is genuinely out of the gate path, so H1's precedent is literally satisfied. Routing and the store-path rule are now right. Supersedes accurate.
- [high] lineage.session_end is the one pin site that did NOT get the store-else-legacy fallback (04:274's row stands, unnamed in Supersedes), so after F3b-1 and before F3b-4 every linked /clear pins None → the successor falls to a foreign header with no digest — 5039's finding 1 verbatim, reintroduced for three merges. Pass: pin the store file's version if it exists, else the legacy file's, through the unchanged owned_version (the same shape the fork site already got); resolve_manifest needs no change.
- [high] F3b-3 landing before F3b-4 makes a session freeze its OWN manifest: the copy creates a store file, the stamp target then prefers it, the skill (still pre-F3b-4) keeps writing the repo file, and every later SessionStart injects the frozen copy with no signal. This answers the librarian's question 5: the store-else-legacy target is the right design (it keeps H1's tests unchanged) but wrong as ordered. Pass: swap to F3b-1 → F3b-2 → F3b-4 (after F1) → F3b-3 → F3b-5; the reviewer walked all four merge points green under that order.
- [medium] F3b-1's acceptance ("byte-for-byte as main") contradicts F3b-1's own legacy_notice line; scope the acceptance.
- [medium] the store-containment identity test is still unspecified (round-1 low): state realpath-prefix containment plus "sid is the FIRST component under the store"; note safe_sid is idempotent (an implementer will otherwise double-hash); skip adopting a session's own store file on a full Read.
- lows: two citation slips; F3b-3's test cases replace 04's rather than adding to them (name it in Supersedes); F3b-5's tier under decision-65 answer (b) is an opus implementer signal, since the stub is written by the skill.
dispatch: planner opus — plan fix round 2 (resume); revision as serial 06 with Supersedes

## Plan fix round 2 (opus) — serial 06_pin-fallback-and-feature-order.md (258 lines)
High 1 closed: session_end gets the same store-else-legacy shape as the fork site and the stamp step, pinning through the unchanged owned_version; resolve_manifest untouched (the successor's step 2 misses, falls to the legacy arm, is_ours matches the carried pin, linked_clear_pred still returns the predecessor) — a four-row table walks that chain at each merge so FM1 cannot reappear. New Confirmed Assumption: all four pin sites resolve alike, and a pin is a SEAL, not an address. High 2 closed by order: F3b-1 → F3b-2 → F3b-4 (after F1) → F3b-3 → F3b-5, all four merge points walked; F3b-3 is now narrower and later (it rescues only sessions that checkpointed pre-F3b-4 and have not since, and its copy fires only from inside the legacy arm). Residual found while walking the new order and guarded: a long-running session still holding pre-F3b-4 skill text could write the repo file after its copy exists — the stamp step refuses when a legacy manifest with the same session: has a strictly newer mtime. Mediums: F3b-1's acceptance scoped to byte-for-byte modulo the legacy_notice line; the containment test specified (realpath prefix with an os.sep guard so handoff-old cannot pass, the sid is the FIRST component of relpath, safe_sid verified idempotent so no double-hashing, and a session never adopts its own store file). Security section restored. Lows: four citation fixes; Supersedes states F3b-3's cases replace 04's; F3b-5 under decision 65(b) is opus/opus because the stub is written by the skill. New tests pin each fix, including a linked /clear with no store file anywhere (fails on 05 as written).
librarian on the planner's new question (should the stamp guard survive the legacy retirement): no — it is transitional, and the scratchpad-manifest warning already covers the permanent version.
dispatch: plan reviewer opus — plan-review round 3 (resume), delta only, on serial 06

## Plan review round 3 — NEEDS_CHANGES, scoped to F3b-3 (opus) on serial 06
Both round-2 highs closed. The reviewer walked the /clear chain BY HAND at all four merge points, including the no-store-file case: session_end takes the legacy fallback and pins through the unchanged owned_version, step 2 misses, step 4 reads the repo file live, is_ours matches the lineage clause, linked_clear_pred returns the predecessor and the full tier + digest run. Both boundary cases also checked. F3b-1, F3b-2 and F3b-4 are CLEAR to dispatch against 06 as it stands.
- [medium, F3b-3 only] the new stamp guard refuses where it should only warn, and the false positive propagates: mtime on the repo file moves for reasons unrelated to a Step 4b write (git checkout/pull/branch switch/stash restore under trackInHost, a fresh worktree checkout), so the store file keeps its <stamped> placeholders; at the next /clear, session_end reads owner "<stamped>", owned_version is False, pin = None, and the successor falls to a foreign header with no digest — the exact failure this serial exists to prevent. There is also an uncovered false negative (a session that writes the repo file and never marks). Pass: warn AND stamp anyway; optionally key the signal on content (the copied sha) rather than mtime, and compare against this_session(...) only when it passes _SAFE_SID.
- lows: containment should reject rp == root and a first component of "." or ".."; require the relpath to be exactly <sid>/HANDOFF.md (deeper paths currently fail safe but silently and for the wrong reason); say that realpathing root is what makes /home/claude/... and /home/rt/... the same store; name the F3b-3 file-list row in Supersedes (05 read as exhaustive).
- notes: probes 1 and 2 closed and stronger under the new order; 06:88-91 is honest about the cost of F3b-3 last (a session that checkpointed pre-F3b-4 and never again loses memory to a foreign header when a peer overwrites the shared file — not a regression, main behaves identically); safe_sid idempotence verified; Security earns its place; Supersedes accurate.
dispatch: planner opus — plan fix round 3 (resume), serial 07, F3b-3 only

## Plan fix round 3 (opus) — serial 07_f3b3-warn-and-stamp.md (198 lines), scoped to F3b-3
The guard warns and stamps anyway, so session: is never left as <stamped> and the traced cascade cannot happen. The signal is keyed on content, not mtime: F3b-3 records legacy_copy = {sha, path, at} with the copy and fires only when the legacy file's manifest_sha differs, so a checkout/pull/branch switch/stash restore that leaves bytes alone is silent. Ownership compares against this_session(sid, environ) and only when it passes _SAFE_SID. One deliberate step past the instruction, flagged with the simpler variant named: on a real signal the store copy is refreshed from the legacy file before the stamp, so the stamp dates the bytes the session actually wrote (new non-blocking OQ for the F3b-3 reviewer; recommendation refresh). mtime is stated per operation: the first copy preserves the source's mtime; a refresh does not, because the stamp follows immediately. New Confirmed Assumption so it cannot recur: a guard on the write path may warn, never refuse. The false negative (a session that writes the repo file and never marks) is named and accepted — the standing channels are F3b-1's legacy_notice and, after F3b-4, the skill's own Step 4b text. Containment gains four clauses (reject rp == root, reject a first component of . or .., require exactly <sid>/HANDOFF.md, and name realpath(root) as what makes /home/claude and /home/rt the same store — verified in this container). Bookkeeping: Supersedes names both partial F3b-3 file rows; § Files to Modify carries the full list. F3b-1, F3b-2 and F3b-4 are unchanged by this serial and clear to dispatch against 04–06.
dispatch: plan reviewer opus — delta review of serial 07
dispatch: implementer opus — F3b-1 (the reviewer cleared it against 04–06)

## Plan review round 4 — NEEDS_CHANGES, scoped to F3b-3's guard (opus) on serial 07
Warn-and-stamp is right and closes the round-3 cascade (restamp always writes session: want, so the placeholder never reaches session_end). But the planner's self-chosen refresh-before-stamp is wrong three ways: (1) the guard's own ownership test (skip unless session: equals want) excludes its motivating case — a pre-F3b-4 Step 4b write leaves `session: <stamped>`, never want, and nothing ever corrects it, so the guard is inert exactly where it was built to fire; (2) loosening that test to accept a placeholder makes the refresh unsafe, because a PEER's fresh repo file is also `<stamped>` and indistinguishable — refreshing imports a peer's manifest into this session's store, the harm answer 47 removed; (3) the refresh newly admits `<stamped>` bytes into the store file, and a downstream refusal (no frontmatter, or a concurrent change) strands them unstamped, which restarts the round-3 cascade — without the refresh the store file keeps its previously-stamped bytes and the cascade is impossible. A fourth reason: a checkout restoring older different bytes would, under the refresh, import stale memory and relabel it FRESH — what H1 was built to end.
Pass: drop the refresh, take 07's own simpler variant (warn, stamp the store file as it stands), and key the warning on the sha difference ALONE, without the session: test — a warning imports nothing, so it can be true and useful whatever the author.
- [low-medium] the simpler variant's cost is misstated: the first copy preserves the source mtime, so the stamp hits the 30-min window and refuses; the real cost is a store manifest that keeps its old stamp and reads AGED while the just-written content stays stranded in the repo file — honest, not the cascade. The warning already tells the operator to re-run Step 4b against the printed store path.
- lows: the mtime split's conclusion is right but its stated reason is the wrong mechanism (_own_stamp never gates the refresh case; the 30-min window does); the containment tightening is labelled docs-only but adds a branch and three tests to F3b-1 — name the delta; one citation range slip; a line pointing at 06 § Security.
- note for the F3b-3 brief: if the refresh is dropped, stamp_manifest stays a pure file operation with no state write.
F3b-1, F3b-2 and F3b-4 remain CLEAR to dispatch against 06 (plus the three containment tests 07 adds).
dispatch: planner opus — plan fix round 4 (resume), serial 08, F3b-3's guard only

## Plan fix round 4 (opus) — serial 08_f3b3-warn-only.md (196 lines), F3b-3's guard only
The refresh is withdrawn entirely, with all four reasons recorded in Supersedes so it cannot be re-proposed. Final form: skip unless legacy_copy and its path exist; skip when the legacy file's manifest_sha equals the recorded copy sha; otherwise warn (naming the legacy path, the store path and "Step 4b now writes there — rewrite it and run the mark step again"), then fall through to the normal stamp. No session: test, no refresh, no refusal. New Confirmed Assumption: a guard may read and report, it may not import — moving bytes between a shared file and a private store needs an authorship test the transitional <stamped> state cannot supply. Load-bearing correction: round 3's cascade is closed by the COPY's own condition (owner == sid), not by the guard, so the store file always carries already-stamped bytes with a real owner and the pin is never None; the guard is an operator signal, not a correctness mechanism. Cost restated correctly (a copy older than the window fails the 30-min check, so the store manifest reads AGED while the new content stays stranded — honest, not the cascade, and both warnings print together). Mtime mechanism and the citation fixed; stamp_manifest stays a pure file operation with a test asserting no state write, and the warning repeating is the desired standing signal. A line points at 06 § Security. Tests replace 07's bullet, including a peer-rewrite case asserting the store bytes are unchanged byte for byte.
F3b-1, F3b-2 and F3b-4 unchanged; 08 names the containment delta properly (one branch, three tests) as an addendum for F3b-1's implementer, which is already dispatched.
dispatch: plan reviewer opus — delta check of serial 08 (the last)

## Plan delta check round 5 — CLEAR (opus) on serial 08; serials 04–08 are the PLAN OF RECORD
The load-bearing claim verified directly: F3b-3 copies only from the owner == sid arm, which requires owned_version's first clause with both fields non-empty _SAFE_SID strings, so a copied store file always carries this session's real owner; session_end reads it, the pin is set, and the successor keeps the full tier and digest. The cascade is closed before the guard is reached — the guard is an operator signal, not a correctness mechanism, which is what makes "warn, never refuse, never import" obviously right. Composition check: 04→08 with each Supersedes applied yields one coherent plan and no contradiction left standing. F3b-1 (with 08's addendum), F3b-2 and F3b-4 clear to dispatch.
Lows for F3b-3's brief (it lands last, so there is room):
- the cascade Confirmed Assumption is broader than true — it holds for COPIED store files, not for a freshly written one that keeps <stamped> after a malformed write (main's existing behaviour, not a regression). Four words fix it.
- [low-medium] the warning's text misinstructs in the peer case, which is this repo's normal case (14 live peers shared the file): dropping the session: test makes a peer's rewrite print "rewrite the manifest there and run the mark step again" on EVERY checkpoint for the life of the session, with nothing that clears it. Fix: state the fact and make the instruction conditional; the guard may READ session: to choose wording without importing anything — "a guard may read and report, it may not import" bars moving bytes, not reading a field.
- the cost statement's label claim is wrong: liveness returns AGED only past 24h, on code drift, on head divergence or a future stamp, so a copy 31 min–24h old in a quiet repo reads FRESH with a stale body. This strengthens the case for the warning (it is the only signal in that band) but the plan must say so.
- two off-by-one citations in the paragraph that corrects a citation.
dispatch: implementer opus — F3b-2 (stamp store-else-legacy, session_warnings, handoff_path.py)
F3b-1 impl r0 DONE eaad03c (opus): the store (MANIFEST_NAME, handoff_root, manifest_path), the containment helper manifest_sid, the optional lineage at; rehydrate gains legacy_manifest_path, read_store_manifest, own_manifest, _sealed, resolve_manifest, resolve_top, legacy_notice, with adopt_fork_state re-pointed and main's call order changed; session_end pins store-else-legacy; post_read accepts another session's store manifest and never its own; read_list's top contract documented. 644 context-guard tests (was 615); 33 new tests fail on main's hooks, including the linked-/clear-with-no-store-file regression guard and all five sources of the byte-compare. Deviations: built 05/06's first-hit-wins skip (a broken seal falls through to legacy) rather than 04's "drops to the header", per the merge-point table; the byte-compare strips the notice from both sides so it stays true after the merge, with the notice asserted separately; one holds test now cuts at the part boundary. OQs: the notice wording is constrained by two existing assertions (assertNotIn "moved"/"adopt"); resolve_top expands ~ before the isdir test.
dispatch: reviewer opus — F3b-1 (hook logic deciding what memory a session is given)
F3b-2 impl r0 DONE_WITH_CONCERNS 07786c2 (opus): store_manifest_path + stamp_target (store-else-legacy), used by stamp_manifest and session_warnings; _claimable skipped on the store arm, unchanged on the legacy one; top: added to the stamped machine fields; new hooks/handoff_path.py carrying --path (no state, never calls L.mark_checkpoint); handoff-format updated and the F3b caveat deleted per 04's Supersedes. 630 tests (615 untouched + 15 new, all 15 fail without the change). Deviations: F3b-1 is not on main, so store_manifest_path bridges via getattr to L.manifest_path when present and otherwise computes the byte-identical path, with a docstring saying to delete the fallback at F3b-1's merge (it lives only in mark_checkpoint.py, which F3b-1 does not touch, so no textual conflict); top: is stamped per the plan though the dispatch prompt did not name it; stamp_target is public so tests can pin the resolve order.
librarian on its open questions: (1) the handoff_path.py argv contract is accepted as chosen (--path required, id optional, env wins, exit 1 with no id anywhere) and goes into F3b-4's brief; (2)/(3) README, CLAUDE.md layout, SKILL.md and the playbook stay F3b-4's, as the plan assigns; (4) accepted — an unreadable store manifest reports "not stamped: <ExceptionType>" rather than falling back to the repo file, because the store path IS the target once it exists; the reviewer may dispute.
dispatch: reviewer opus — F3b-2 (the mark step and its resolve order)
F3b-2 review r1 (opus) at 07786c2: NEEDS_CHANGES. H1 stays closed (env X + argv Y resolves to X's store path, Y's file byte- and mtime-identical); the gate record is literally main's (lib_context not in the diff; argv contract diffed identical for five cases); the bridge is byte-identical to F3b-1's manifest_path over 9 ids including hostile and mangled ones, and cannot be picked up by accident from main; top: correct on both arms with one transitional re-stamp; handoff_path writes nothing and creates no directory. 630 tests, all 15 new fail on main.
- [medium] mark_checkpoint.py:213 — stamp_target decides is_store from os.path.exists alone and _write_atomic writes through realpath, so a symlink at the store path (or at the <sid>/ directory) makes an arbitrary file "ours by path", skips _claimable, and rewrites it silently. Probed: a symlink to a repo manifest carrying session: PEER is rewritten to session: S, rc 0, empty stderr, where main refuses it. The store directory is shared by every session in the container, so the symlink need not be the victim's own doing. Pass: verify identity before collapsing the claim test — after F3b-1, L.manifest_sid(p) == L.safe_sid(want) (verified to reject both a symlinked file and a symlinked dir); before it, compare realpath(p) against realpath(handoff_root())/safe_sid(want)/HANDOFF.md. Anything failing falls through to the legacy arm; plus a test.
- lows: SKILL.md:142 still names four machine fields where handoff-format now says five, and the playbook still says "run from the repo" — F3b-4's brief must carry both lines explicitly, because the drift starts here; an unreadable store manifest surfaces only as "not stamped: PermissionError" naming neither the file nor the new target, and session_warnings swallows it (the reviewer AGREES with the librarian that not falling back is right — falling back would put a fresh stamp on the shared repo file, the claim H1 removed); handoff_path prints a path for a never-existing session when the env id is unset and reads "--path --path" as an id.
- notes: top: re-stamps any pre-F3b-2 manifest once, which invalidates an outstanding Read-adoption pin (one-time, fail-safe); the store arm stops warning about a peer-owned repo manifest once a store file exists — correct per the acceptance, and the last in-session signal about the shared file before F3b-3's guard; stamp_target is called twice per run, a harmless TOCTOU window.
dispatch: implementer opus — fix round 1 (resume)
F3b-1 review r1 (opus) at eaad03c: NEEDS_CHANGES. Extended parity: main's rehydrate vs the branch's over 10 fixtures × 5 sources (owned/ownerless/foreign/landed/.claude-sandbox/prior-state/hostile sid) — identical in all 50 modulo one notice line, well beyond the branch's own test. 11 resolve-order attacks all fail to break it (a peer's store manifest with no link is invisible; a broken seal plus a foreign legacy file gives the header and neither body; hostile lineage sids inject nothing; two sessions never cross). 16 mutants, 13 caught. All seven Checks OK; the linked-/clear-with-no-store-file guard asserts the full tier and the predecessor's digest, with both pin-site directions caught.
- [medium] rehydrate.py:983,988 — the LINK-KEYED resolve is untested: keying steps 2/3 on pin["owner"]/ad["owner"] instead of the linked sid leaves all 644 green. This is exactly the rule 05 added to close plan-review round 1's medium. The reviewer's fixture (S's lineage names A, the pin's owner is B, B has that store file, A has none) shows the owner-keyed mutant injects B's PRIVATE MANIFEST IN FULL into S while the as-built code injects nothing. Pass: that fixture as a test, both arms, asserting an empty injection.
- [medium] rehydrate.py:1101-1104 — "own is ours by the PATH, no sha and no owner comparison" is untested: replacing it with is_ours leaves the suite green, because every store fixture writes its own sid. The discriminating case arrives with F3b-4: between Step 4b's write and the mark, the store file carries session: <stamped>, and the mutant then injects the FOREIGN HEADER — the session loses its own memory. Pass: a store manifest whose session: is <stamped> (and one naming another session) injects full.
- lows: resolve_top expands ~ before the isdir test, so top: "~" would run every derived check against the reader's HOME (unstated, untested either way); the notice says the skill "no longer writes one there", false until F3b-4 — this repo has a live .claude-sandbox/HANDOFF.md, so every session would be told a present-tense falsehood (plan residual, conditional phrasing costs nothing); the byte-compare guard fixes one ownership arm where 06 asks for every one (the reviewer ran the others by hand, all pass). nit: the legacy arm runs git rev-parse twice per SessionStart.
- reviewer answers the brief's seven questions: no path hands a session a manifest that is not its own, its lineage's or its adoption's; containment sound (the os.sep guard is defence in depth, not load-bearing); the fork pin works and nothing is read before it is set; the parity test cannot hide a regression, only narrow one; neither deviation is one (05:154-176 and 06:117 supersede 04's "drops to the header", and the notice wording is genuinely boxed in by two existing assertions); the linked-/clear guard holds.
- OQ 10 answered by the reviewer, concurring: the legacy notice does NOT fire for a session that already has a store manifest.
dispatch: implementer opus — fix round 1 (resume)
F3b-2 fix r1 DONE 8092d28 (opus): own_store_manifest(path, sid) gates the claim-test collapse — L.manifest_sid(p) == L.safe_sid(want) once F3b-1 lands, and realpath(p) == realpath(handoff_root())/<safe_sid>/HANDOFF.md until then, the same bridge shape and getattr as store_manifest_path; anything failing falls through to the legacy arm and its unchanged _claimable, so the reviewer's symlink probe now ends in "not stamped: … has `session: PEER`", byte-for-byte main's answer, with the repo file's bytes and mtime intact. New TargetUnreadable names the path. handoff_path rejects an argument starting with "-". 633 tests; 4 new/extended tests fail on the reviewed sha. Declined with reasons (both accepted): handoff_path stays state-free (a live session always has a state file, so the check buys nothing and costs the property); session_warnings keeps swallowing TargetUnreadable because stamp_manifest already named the file on the same run. Low (c) recorded in the commit body: the first mark adds top: to a pre-F3b-2 manifest, so an outstanding pin or adoption stops matching once — the holder drops to the foreign header and the next checkpoint re-pins; one-time and fail-safe.
dispatch: reviewer opus — review r2 (resume)
F3b-1 fix r1 DONE 2568e95 (opus): the two mutant-killing tests (link-keyed lookup over both arms with a control; own-is-ours-by-path over <stamped>, another id and ownerless) plus a top:-verbatim unit test; resolve_top now takes top: verbatim and requires it absolute (a relative value resolves against whatever dir the hook started in — the same class of error as ~); the notice re-worded and computed BEFORE the tiers so the trim budget pays for it (the existing CAP test failed at 9,072 and is now the guard); git_top cached. 647 tests; all three mutants now fail. OQs: git_top caches per process (rehydrate is one-shot; fixtures use unique temp paths); the six-fixture distinctness assertion could become brittle if the LANDED tier ever stops injecting a body.
dispatch: reviewer opus — review r2 (resume)
F3b-2 review r2 (opus) at 8092d28: CLEAR. Symlink probes all closed — a symlinked file, a symlinked <sid>/ dir, a symlink landing back inside the store under another sid, one outside the store, and a broken one all fall to the legacy arm with main's own refusal and the peer's file byte- and mtime-identical; both controls still stamp, including CLAUDE_CONFIG_DIR reached through this container's /home/claude symlink. The bridge agrees with F3b-1's manifest_sid on all 22 path/sid cases, and main exposes neither name, so the getattr switch at the merge is clean. H1 closed and the gate record literally main's (argv contract identical over five cases). TargetUnreadable names the file for both PermissionError and IsADirectoryError with no fallback. handoff_path rejects "-" ids. Low carried for F3b-4's brief: SKILL.md:142 still names four machine fields where handoff-format says five, and the playbook still says "run from the repo". Notes: a hard link cannot be seen by realpath, but os.replace severs it and the peer's bytes survive — the store directory is the trust boundary; own_store_manifest returns False on any exception, the fail-safe direction.
Review result: 2 rounds, 1 fix round; impl opus, review opus. HELD for the plan's order: F3b-1 lands first (it is in review r2), so main is never left with the stamp targeting a store the resolve cannot reach.
F3b-1 review r2 (opus) at 2568e95: CLEAR. Both round-1 survivors killed, with a control in each new test so they cannot pass by accident; 20 mutants run, the only greens are the os.sep guard (defence in depth — outside paths are rejected by the component tests anyway) and an uncached git_top (perf-only). The notice-in-budget change is a real guard (M18 caught). top: absolute-only refuses nothing legitimate; the /home/claude ↔ /home/rt spelling still resolves. Parity re-run over 10 fixtures × 5 sources, identical to main modulo the notice; all 11 resolve-order attacks still fail.
- [low, for F3b-3/F3b-4's brief] subtracting the notice from the trim budget costs PROTECTED content in trim's extreme regime: measured 8,600 chars on main vs 8,193 on HEAD-minus-notice, the 407 coming off ## Doing because the protected part alone exceeded the budget and trim ended at [:budget]. In the ordinary over-budget case it correctly comes off Scrolls/Aware-of/Next. The byte-compare stays green only because every fixture is small enough not to trim, so the acceptance now reads "byte-for-byte modulo the notice, for manifests that do not trim" — unstated. Either keep the subtraction, say so in the acceptance and add a trimming-size fixture, or drop it (the output is cut at CAP+900 = 9,900 under a 10,000 cap and the pre-fix worst case was ~9,130, so the notice fits in headroom the design never spent).
Review result: F3b-1 2 rounds/1 fix round; F3b-2 2 rounds/1 fix round; impl opus, review opus both.
land: F3b-1 merged 92c9738 (seven Checks green on main). F3b-2 conflicts with it — its implementer merges main and resolves, deleting the getattr bridge's pre-merge arm now that L.manifest_path and L.manifest_sid exist.
dispatch: implementer opus — F3b-2 conflict round (resume)
F3b-2 conflict round DONE f73f951 (merge) + 4572e17 (opus): one conflict, test_lineage.py, both sides appended, kept in plan order with no name defined twice; both getattr bridges deleted now that L.manifest_path and L.manifest_sid exist, with the try/except kept deliberately as a write-path guard; the legacy arm is unchanged by F3b-1 (read_manifest still returns (path, top, text)). 716 context-guard tests.
NEW finding from the implementer, filed as context-guard-the-manifest-read-path-tak-0836 (P1): F3b-1's READ path (read_store_manifest/own_manifest) takes the store file on os.path.exists alone with no identity test, so a symlink planted at <store>/<sid>/HANDOFF.md would be injected as that session's memory — the write-path equivalent was F3b-2's r1 medium and is guarded. F3b-1 is already on main.
dispatch: reviewer opus — F3b-2 resolution verify (resume)
F3b-2 resolution verify (opus) at 4572e17: CLEAR. The resolution checked mechanically — every file under plugins/ is byte-identical to one parent except test_lineage.py, where the merge is 232 pure additions over main's side with no deletion, an AST pass finds no duplicate among 28 module-level names, and no marker remains. The cleanup preserves behaviour across all 22 path/sid cases and all nine ids; every symlink probe still closed; H1 closed and the gate record untouched on the merged code; the legacy arm is genuinely unchanged by F3b-1 (both placements probed end to end). Low carried for F3b-4's brief: SKILL.md:213 (moved from :142) still names four machine fields, and the playbook still says "run from the repo" — three citations now.
answer 65: a (operator 2026-09-22)
